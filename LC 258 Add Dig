class Solution:
    def addDigits(self, num: int) -> int:
        a=str(num)
        q=0
        w=0
        e=0
        for i in a:
            q+=int(i)
        for j in str(q):
            w+=int(j)
        for k in str(w):
            e+=int(k)
        return e
        
